import Foundation

enum FeatureFlags {
    /// Audio captions: TTS audio playback during slideshow for images/GIFs with associated audio files.
    /// Set to false to hide all audio caption UI elements.
    static let audioCaptions = true
}
